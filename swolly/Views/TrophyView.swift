import SwiftUI

struct TrophyView: View {
    var body: some View {
        NavigationView {
            Text("Trophy View")
                .navigationBarTitle("Trophies", displayMode: .inline)
        }
    }
}
